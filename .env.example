# Fill in your cloud MySQL credentials here (e.g. from Railway.app)
# Copy this file and rename it to .env — never push .env to GitHub!

DB_HOST=your_host_here
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_username
DB_PASSWORD=your_password
